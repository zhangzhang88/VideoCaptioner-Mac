激活虚拟环境
source venv/bin/activate 

运行主程序
python main.py

# 一键启动脚本
创建以下脚本并保存到桌面，命名为 `启动VideoCaptioner.command`：

```bash
#!/bin/bash

# 进入项目目录
cd /Users/world/Downloads/code/VideoCaptioner-master

# 激活虚拟环境
source venv/bin/activate

# 运行程序
python main.py

# 如果想在程序退出后保持终端窗口打开，可以取消下面这行的注释
# read -p "按任意键退出..."
```

然后在终端中执行以下命令使脚本可执行：

```bash
chmod +x ~/Desktop/启动VideoCaptioner.command
```

双击桌面上的 `启动VideoCaptioner.command` 文件即可一键启动VideoCaptioner程序。

